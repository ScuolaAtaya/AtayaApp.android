<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_navigation"
    app:startDestination="@id/nav_italiano">

    <fragment
        android:id="@+id/nav_italiano"
        android:name="it.mindtek.ruah.fragments.FragmentUnits"
        android:label="Italiano">

        <argument
            android:name="category"
            android:defaultValue="italiano"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_sicurezza"
        android:name="it.mindtek.ruah.fragments.FragmentUnits"
        android:label="Sicurezza">

        <argument
            android:name="category"
            android:defaultValue="sicurezza"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_lingua"
        android:name="it.mindtek.ruah.fragments.FragmentUnits"
        android:label="Lingua">

        <argument
            android:name="category"
            android:defaultValue="lingua"
            app:argType="string" />
    </fragment>
</navigation>